Объекты размещаются в динамической памяти (heap) при выполнении оператора new.

Java "работает" не с самими объектами, а со ссылками на них.

Прямое сравнение объектов (==) не имеет смысла, так как сравниваются их адреса.

Для сравнения объектов по значению используется метод equals.