Варианты использования:

int i = 0;
if (логическое значение) {
    i = 1; // логическое значение верно
} else {
    i = 2; // логическое значение ложно
}
int i = 0;
if (логическое значение) {
    i = 1; // логическое значение верно
}
i = 2; // этот код выполняется всегда
if (логическое значение)
    i = 1; // логическое значение верно

i = 2; // этот код выполняется всегда
int i = 0;
if (логическое значение 1) {
    i = 1; // "логическое значение 1" верно
} else if (логическое значение 2) {
    i = 2; // "логическое значение 2" верно
} else if (логическое значение 3) {
    i = 3; // "логическое значение 3" верно
} else {
    i = 4; 
    // "логическое значение 1" НЕ верно И
    // "логическое значение 2" НЕ верно И
    // "логическое значение 3" НЕ верно
}